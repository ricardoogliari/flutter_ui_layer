name: atitus_flutter_ui_layer
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  provider: ^6.1.5
  go_router: ^14.6.2
  intl: ^0.20.2
  http: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  json_serializable: ^6.9.5
  mockito: ^5.4.6

flutter:
  uses-material-design: true
